import Foundation
import CoreData

class ProfileStorageService {
    // TODO: Реализация CRUD для профиля через CoreData
    func save(profile: Profile) throws {
        // Заглушка сохранения
        throw NSError(domain: "NotImplemented", code: 0)
    }
    func load() throws -> Profile? {
        // Заглушка загрузки
        throw NSError(domain: "NotImplemented", code: 0)
    }
}
